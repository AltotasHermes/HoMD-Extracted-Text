if (BoxMode == "Event")
            oPlayer.LastVisitedForSavePurposes = "Misericord"
            FileSelected = "SaveFile0.homd"
        else if (DialogueScreen == "FirstScreen")
                ("M")
                ("ISERICORD")
                ("A forgotten ruin of a city, its cathedrals hollowed, its temples shattered, its towers fallen. Nothing but broken stone.")
                    (("New location visited. " + ApprehensionsString), "Logbook", 4)
                        FileSelected = "SaveFile0.homd"
                    if (oPlayer.Lookout == "Spire")
                        ("Wander the ruins with Spire.")
                    if (oPlayer.Navigator == "Peter McNally")
                        ("Peter closes his eyes and steps from the kinetopede.", '"' + "Tump, tump, tump," + '"' + " he mutters under his breath as he walks.")
                    ("Scavenge the ruins.", "Spiderweb cracks run through every teetering edifice.", "")
                else if (oPlayer.LastVisited != "Misericord" && global.Misericord == 0)
                    ("Scavenge like an expert.", "Spiderweb cracks run through every teetering edifice.", "[This option is available because you have the Scavenger childhood.]")
                ("Inside the kinetopede.", "Here you can find all miscellaneous options.")
                ("M")
                ("ISERICORD")
                ("A forgotten ruin of a city, its cathedrals hollowed, its temples shattered, its towers fallen. Nothing but broken stone.")
                    if (oPlayer.Lookout == "Spire")
                        ("Wander the ruins with Spire.")
                    if (oPlayer.Navigator == "Peter McNally")
                        ("Peter closes his eyes and steps from the kinetopede.", '"' + "Tump, tump, tump," + '"' + " he mutters under his breath as he walks.")
                    if (oPlayer.LastVisited != "Misericord" && global.Misericord == 0)
                        ("Scavenge the ruins.", "Spiderweb cracks run through every teetering edifice.", "")
                else if (oPlayer.LastVisited != "Misericord" && global.Misericord == 0)
                    ("Scavenge like an expert.", "Spiderweb cracks run through every teetering edifice.", "[This option is available because you have the Scavenger childhood.]")
                ("Inside the kinetopede.", "Here you can find all miscellaneous options.")
        else if (DialogueScreen == "SecondScreen" && LeavingTheCity == 1 && TooHeavyToLeaveTheCity == 0)
            ("I")
            ("NTO THE DARK")
            ("Which direction will you go?")
            ("On second thoughts...", "There are matters here that still merit attention.", "#"0, 0.8)
            ("Pore over the map.", "Orient yourself before you launch off into the ravenous dark.")
                    BoxMode = "Map"
            ("North.", "An archive of all knowledge.")
                oPlayer.LastVisited = "Misericord"
                LeavingSpecialRoomDialogueBox("North")
            ("West.", "Locked doors. Disappointment and hopelessness.")
                oPlayer.LastVisited = "Misericord"
                LeavingSpecialRoomDialogueBox("West")
            ("East.", "Godkillers that way. Not a cheery bunch.")
                oPlayer.LastVisited = "Misericord"
                LeavingSpecialRoomDialogueBox("East")
            ("South.", "Uncharted and scary lands.")
                oPlayer.LastVisited = "Misericord"
                LeavingSpecialRoomDialogueBox("South")
        else if (DialogueScreen == "SecondScreen" && LeavingTheCity == 1 && TooHeavyToLeaveTheCity == 1)
        else if (DialogueScreen == "SecondScreen" && SpireQuest == 1)
        else if (DialogueScreen == "SecondScreen" && PeterQuest == 1)
        else if (DialogueScreen == "SecondScreen" && WanderTheStreets == 1)
        else if (DialogueScreen == "SecondScreen" && MiscellaneousEvent == 1)
        else if (DialogueScreen == "SecondScreen" && MiscOptions == 1)
            ("C")
            ("APTAIN'S QUARTERS")
            ("The machine greets you with a steady, soothing hum. In your quarters, you can safely decide what side activity to pursue. Deal with prisoners? Drink something? Operate on your own memories, perhaps?")
            ("Return to location.", "")
        else if (DialogueScreen == "LastScreen")
            ("T")
            ("EST")
            ("Successful test.")
            ("Return.", "", "")
    draw_text(((view_wport[(0 << 0)] * 0.5) - 3), ((view_hport[(0 << 0)] * 0.75) - 3), "Misericord")
    draw_text((view_wport[(0 << 0)] * 0.5), (view_hport[(0 << 0)] * 0.75), "Misericord")
