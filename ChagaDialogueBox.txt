if (BoxMode == "Event")
            oPlayer.LastVisitedForSavePurposes = "Chaga"
            FileSelected = "SaveFile0.homd"
        else if (DialogueScreen == "FirstScreen")
                ("C")
                ("HAGA")
                ("An ancient fungal ruin.")
                if NextButton("A mycena city was built on this vast shelf-fungus once, a city whose occult powers still defy understanding. The shelf collapsed long ago, sloughing across the floor.", 1)
                        (("New location visited. " + ApprehensionsString), "Logbook", 4)
                            FileSelected = "SaveFile0.homd"
                    if (oPlayer.LastVisited != "Chaga" && ConcernsReturnNumberOfItems(80) == 2)
                        if (oPlayer.SurgeonWoundsTracker > 0 && oPlayer.Surgeon == "Jhang Ba Sho")
                            ("Search for the Lady.")
                                
                        if (oPlayer.LastVisited != "Chaga" && global.Chaga == 0)
                            ("Explore the ruin.", "Eerie fungus-phosphorence plays across your kinetopede, painting the wall with dancing shadows.", "")
                                
                    else if (oPlayer.LastVisited != "Chaga" && global.Chaga == 0)
                        ("Scavenge the ruin like an expert.", "Eerie fungus-phosphorence plays across your kinetopede, painting the wall with dancing shadows.", "[This option is available because you have the Scavenger childhood.]")
                            
                    ("Inside the kinetopede.", "Here you can find all miscellaneous options.")
                        
                ("C")
                ("HAGA")
                ("An ancient fungal ruin. The shelf collapsed long ago, sloughing across the floor.")
                if (oPlayer.LastVisited != "Chaga" && ConcernsReturnNumberOfItems(80) == 2)
                    if (oPlayer.SurgeonWoundsTracker > 0 && oPlayer.Surgeon == "Jhang Ba Sho")
                        ("Search for the Lady.")
                            
                    if (oPlayer.LastVisited != "Chaga" && global.Chaga == 0)
                        ("Explore the ruin.", "Eerie fungus-phosphorence plays across your kinetopede, painting the wall with dancing shadows.", "")
                            
                else if (oPlayer.LastVisited != "Chaga" && global.Chaga == 0)
                    ("Scavenge the ruin like an expert.", "Eerie fungus-phosphorence plays across your kinetopede, painting the wall with dancing shadows.", "[This option is available because you have the Scavenger childhood.]")
                        
                ("Inside the kinetopede.", "Here you can find all miscellaneous options.")
                    
        else if (DialogueScreen == "SecondScreen" && LeavingTheCity == 1 && TooHeavyToLeaveTheCity == 0)
            ("I")
            ("NTO THE DARK")
            ("Which direction will you go?")
            ("On second thoughts...", "There are matters here that still merit attention.", "#"0, 0.8)
                
            ("Pore over the map.", "Orient yourself before you launch off into the ravenous dark.")
                    BoxMode = "Map"
            ("North.", "A giant pile of garbage.")
                oPlayer.LastVisited = "Chaga"
                LeavingSpecialRoomDialogueBox("North")
            ("West.", "Frigid wastes where nothing survives for long.")
                oPlayer.LastVisited = "Chaga"
                LeavingSpecialRoomDialogueBox("West")
            ("East.", "Nothing but Chimeric Empire which better be avoided.")
                oPlayer.LastVisited = "Chaga"
                LeavingSpecialRoomDialogueBox("East")
            ("South.", "To the humble town of carchars.")
                oPlayer.LastVisited = "Chaga"
                LeavingSpecialRoomDialogueBox("South")
        else if (DialogueScreen == "SecondScreen" && LeavingTheCity == 1 && TooHeavyToLeaveTheCity == 1)
        else if (DialogueScreen == "SecondScreen" && WanderTheStreets == 1)
        else if (DialogueScreen == "SecondScreen" && JhangQuest == 1)
        else if (DialogueScreen == "SecondScreen" && MiscellaneousEvent == 1)
        else if (DialogueScreen == "SecondScreen" && MiscOptions == 1)
            ("C")
            ("APTAIN'S QUARTERS")
            ("The machine greets you with a steady, soothing hum. In your quarters, you can safely decide what side activity to pursue. Deal with prisoners? Drink something? Operate on your own memories, perhaps?")
            ("Return to location.", "")
                
        
                
    draw_text(((view_wport[(0 << 0)] * 0.5) - 3), ((view_hport[(0 << 0)] * 0.75) - 3), "Chaga")
    draw_text((view_wport[(0 << 0)] * 0.5), (view_hport[(0 << 0)] * 0.75), "Chaga")
