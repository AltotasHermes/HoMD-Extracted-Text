if (BoxMode == "Event")
            oPlayer.LastVisitedForSavePurposes = "Vex"
            FileSelected = "SaveFile0.homd"
        else if (DialogueScreen == "FirstScreen")
                ("V")
                ("EX")
                ("A pillar of charred flesh that was once a god looms on the horizon. Gouts of smoke hiss from its bleeding seams.")
                if NextButton("The shuffling crowd on the beach are grey-faced addicts, drowning their minds in godsmoke.", 1)
                        (("New location visited. " + ApprehensionsString), "Logbook", 4)
                            FileSelected = "SaveFile0.homd"
                        if (oPlayer.NavigatorWoundsTracker > 0 && oPlayer.Navigator == "Valeria Bell-Roberts")
                            ("Hunt down one of Valeria's runaway ghosts.")
                        if (oPlayer.Recordkeeper == "Bradley Baggs")
                            ("Will Vex impress Bradley?")
                        if (oPlayer.Surgeon == "Char Dvetistek")
                            ("Search for ephialteum.", "Char paces desperately back and forth.")
                    if (oPlayer.LastVisited != "Vex" && global.Vex == 0)
                        ("Gather godsmoke.", "Careful. A single breath of godsmoke can send a man into religious rapture for weeks.", "Vigilance", 36)
                    ("Inside the kinetopede.", "Here you can find all miscellaneous options.")
                ("V")
                ("EX")
                ("A twisted pillar of charred flesh that was once a god looms on the horizon. Gouts of smoke hiss from its bleeding seams.")
                    if (oPlayer.NavigatorWoundsTracker > 0 && oPlayer.Navigator == "Valeria Bell-Roberts")
                        ("Hunt down one of Valeria's runaway ghosts.")
                    if (oPlayer.Recordkeeper == "Bradley Baggs")
                        ("Will Vex impress Bradley?")
                    if (oPlayer.Surgeon == "Char Dvetistek")
                        ("Search for ephialteum.", "Char paces desperately back and forth.")
                if (oPlayer.LastVisited != "Vex" && global.Vex == 0)
                    ("Gather godsmoke.", "Careful. A single breath of godsmoke can send a man into religious rapture for weeks.", "Vigilance", 36)
                ("Inside the kinetopede.", "Here you can find all miscellaneous options.")
        else if (DialogueScreen == "SecondScreen" && LeavingTheCity == 1 && TooHeavyToLeaveTheCity == 0)
            ("I")
            ("NTO THE DARK")
            ("Which direction will you go?")
            ("On second thoughts...", "There are matters here that still merit attention.", "#"0, 0.8)
            ("Pore over the map.", "Orient yourself before you launch off into the ravenous dark.")
                    BoxMode = "Map"
            ("North.", "There you can find carchar's main settlement. It's cozy.")
                oPlayer.LastVisited = "Vex"
                LeavingSpecialRoomDialogueBox("North")
            ("West.", "Villages divided by a river.")
                oPlayer.LastVisited = "Vex"
                LeavingSpecialRoomDialogueBox("West")
            ("East.", "To the Cities. Where dreams and hope die.")
                oPlayer.LastVisited = "Vex"
                LeavingSpecialRoomDialogueBox("East")
            ("South.", "A town of cautious mushroom-folk.")
                oPlayer.LastVisited = "Vex"
                LeavingSpecialRoomDialogueBox("South")
        else if (DialogueScreen == "SecondScreen" && LeavingTheCity == 1 && TooHeavyToLeaveTheCity == 1)
        else if (DialogueScreen == "SecondScreen" && BradleyQuest == 1)
        else if (DialogueScreen == "SecondScreen" && ValeriaQuest == 1)
        else if (DialogueScreen == "SecondScreen" && WanderTheStreets == 1 && WasChallengeSuccessful == 1)
            ("V")
            ("EX")
            ("The crowd silently part for you, and shuffle back in place behind. They are so tightly packed together that the long-starved dead are standing with the living.")
            ("You tighten the straps of your gas-mask nervously and get to work coaxing the smoke into your bottles. No problems this time, thankfully.")
            ("(You passed a Vigilance challenge!)", "Challenge", 0)
            ("Gained x2 Godsmoke Vials", "Drug", 0)
            ("Hurry back.", "Grey tendrils of smoke curl around your feet, as though to pull you out to sea...", "")
        else if (DialogueScreen == "SecondScreen" && WanderTheStreets == 1 && WasChallengeSuccessful == 0)
            ("V")
            ("EX")
            ("The crowd silently part for you, and shuffle back in place behind. They are so tightly packed together that the long-starved dead are standing with the living.")
            ("You get to work coaxing the godsmoke into your bottles. It's frustrating work. The smoke dances coyly from your fingertips. The bottle slips from your hands.")
            ("As you bend to pick it up, one of the silent grey crowd reaches forward and undoes the straps on your gas-mask. Disaster! A searing rush of holy smoke down your throat - your eyes darken -")
            ("(You failed a Vigilance challenge...)", "Challenge", 0)
            ("Gained x1 Godsmoke Vial", "Drug", 0)
            ("Lost some Sanity", "Sanity", 0)
            ("Hurry back, re-applying your gas-mask.", "You find yourself muttering pseudo-religious nonsense. For hours, you are plagued by visions of sunflowers.", "")
        else if (DialogueScreen == "SecondScreen" && MiscellaneousEvent == 1)
        else if (DialogueScreen == "SecondScreen" && MiscOptions == 1)
            ("C")
            ("APTAIN'S QUARTERS")
            ("The machine greets you with a steady, soothing hum. In your quarters, you can safely decide what side activity to pursue. Deal with prisoners? Drink something? Operate on your own memories, perhaps?")
            ("Return to location.", "")
        else if (DialogueScreen == "ThirdScreen" && CharQuest == 1)
        else if (DialogueScreen == "LastScreen")
            ("T")
            ("EST")
            ("Successful test.")
            ("Return.", "", "")
    draw_text(((view_wport[(0 << 0)] * 0.5) - 3), ((view_hport[(0 << 0)] * 0.75) - 3), "Vex")
    draw_text((view_wport[(0 << 0)] * 0.5), (view_hport[(0 << 0)] * 0.75), "Vex")
