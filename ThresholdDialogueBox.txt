if (BoxMode == "Event")
            oPlayer.LastVisitedForSavePurposes = "Threshold"
            FileSelected = "SaveFile0.homd"
        else if (oInventories.CanGoScorthidion == 0 && DialogueScreen == "FirstScreen" && ConcernsReturnNumberOfItems(120) == 10)
        else if (DialogueScreen == "FirstScreen")
                ("T")
                ("HRESHOLD")
                ("This might once have been a city. The floor still bears traces of street, faint indents where walls once stood, the occasional fragment of bone. But little is left except the Panoptych Gate, the final embattled bulwark of reality.")
                    (("New location visited. " + ApprehensionsString), "Logbook", 4)
                        FileSelected = "SaveFile0.homd"
                    if (oPlayer.Lookout == "Spire")
                        ("Wander the ruins with Spire.")
                if (oPlayer.LastVisited != "Threshold" && global.Threshold == 0)
                    ("Wander Threshold.", "Almost all traces of the city are gone.", "")
                ("Inside the kinetopede.", "Here you can find all miscellaneous options.")
                ("T")
                ("HRESHOLD")
                ("This might once have been a city. The floor still bears traces of street, faint indents where walls once stood, the occasional fragment of bone. But little is left except the Panoptych Gate, that final embattled bulwark of reality.")
                    if (oPlayer.Lookout == "Spire")
                        ("Wander the ruins with Spire.")
                if (oPlayer.LastVisited != "Threshold" && global.Threshold == 0)
                    ("Wander Threshold.", "Almost all traces of the city are gone.", "#")
                ("Inside the kinetopede.", "Here you can find all miscellaneous options.")
        else if (DialogueScreen == "SecondScreen" && LeavingTheCity == 1 && TooHeavyToLeaveTheCity == 0)
            ("I")
            ("NTO THE DARK")
            ("Which direction will you go?")
            ("On second thoughts...", "There are matters here that still merit attention.", "#"0, 0.8)
            ("Pore over the map.", "Orient yourself before you launch off into the ravenous dark.")
                    BoxMode = "Map"
            ("North.", "A way back.")
                oPlayer.LastVisited = "Threshold"
                LeavingSpecialRoomDialogueBox("North")
            ("West.", "A way back.")
                oPlayer.LastVisited = "Threshold"
                LeavingSpecialRoomDialogueBox("West")
            ("East.", "A way back.")
                oPlayer.LastVisited = "Threshold"
                LeavingSpecialRoomDialogueBox("East")
                ("South.", "Your doom awaits.")
                    oPlayer.LastVisited = "Threshold"
                    LeavingSpecialRoomDialogueBox("South")
        else if (DialogueScreen == "SecondScreen" && LeavingTheCity == 1 && TooHeavyToLeaveTheCity == 1)
        else if (DialogueScreen == "SecondScreen" && SpireQuest == 1)
        else if (DialogueScreen == "SecondScreen" && WanderTheStreets == 1)
                ("T")
                ("HRESHOLD")
                ("As you wander through crumbling Threshold, you realize that reality is at its thinnest out here. Your tongue has become a lizard, mercifully deceased. Your left eye has migrated to the side of your neck. You better leave quickly, before these changes become permanent.")
                ("There is hardly anything left of the old city. Even the foundations are just pale imprints. Eventually you stumble across a skeleton, miraculously intact, with a strange shimmering shape hovering above its skull.")
                ("Gained x1 Oscillating Prism", "Treasure", 1)
                ("Gained x1 Old Bones", "Cargo", 4)
                ("Return to the kinetopede.", "The air tastes of milk. Your skin is coming up in a strange spiral-shaped rash.", "")
                ("T")
                ("HRESHOLD")
                ("As you wander through crumbling Threshold, you realize that reality is at its thinnest out here. Your tongue has become a lizard, mercifully deceased. Your left eye has migrated to the side of your neck. You better leave quickly, before these changes become permanent.")
                ("There is nothing left of the old city. Even the foundations are just pale imprints. You contemplate the faded ruins and rising dust for a while, transfixed.")
                ("Gained x1 Reflection on Mortality", "Logbook", 3)
                ("Return to the kinetopede.", "The air tastes of milk. Your skin is coming up in a strange spiral-shaped rash.", "")
        else if (DialogueScreen == "SecondScreen" && GatheringNews == 1)
            ("T")
            ("HE LATEST GOINGS-ON")
            News1 = "The Panopytch Gate holds. Old news, perhaps, but welcome."
            ("Gained x1 Current Affairs: Threshold", "Logbook", 4)
            ("Well, well.", "The horrors beyond are held at bay.", "#")
        else if (DialogueScreen == "SecondScreen" && MiscellaneousEvent == 1)
        else if (DialogueScreen == "SecondScreen" && MiscOptions == 1)
            ("C")
            ("APTAIN'S QUARTERS")
            ("The machine greets you with a steady, soothing hum. In your quarters, you can safely decide what side activity to pursue. Deal with prisoners? Drink something? Operate on your own memories, perhaps?")
            ("Return to location.", "")
        else if (DialogueScreen == "LastScreen")
            ("T")
            ("EST")
            ("Successful test.")
            ("Return.", "", "")
    draw_text(((view_wport[(0 << 0)] * 0.5) - 3), ((view_hport[(0 << 0)] * 0.75) - 3), "Threshold")
    draw_text((view_wport[(0 << 0)] * 0.5), (view_hport[(0 << 0)] * 0.75), "Threshold")
