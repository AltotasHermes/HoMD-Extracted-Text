if (BoxMode == "Event")
            oPlayer.LastVisitedForSavePurposes = "Murmurhold"
            FileSelected = "SaveFile0.homd"
        else if (DialogueScreen == "FirstScreen")
                ("M")
                ("URMURHOLD")
                ("A lonely castle on a cliff. A dark history, the details obscure.")
                    (("New location visited. " + ApprehensionsString), "Logbook", 4)
                        FileSelected = "SaveFile0.homd"
                    if (oPlayer.LastVisited != "Murmurhold")
                        NeedsCargoItem("Give the Coda a Cryptoglyphic Tablet.", "A tribute, in exchange for your life.", (("[Requires x1 Cryptoglyphic Tablet.You currently have x" + string(CargoReturnNumberOfItems(58))) + ".]")0, 0.8, HasTablet)
                    if (oPlayer.LastVisited != "Murmurhold")
                        NeedsCargoItem("Give the Coda an Ancient Grimoire.", "A tribute, in exchange for your life.", (("[Requires x1 Ancient Grimoire.You currently have x" + string(CargoReturnNumberOfItems(32))) + ".]")0, 0.8, HasGrim)
                    if (oPlayer.Recordkeeper == "Jack of Quills")
                        ("Creep in search of the Coda.")
                if (oPlayer.LastVisited != "Murmurhold" && global.Murmurhold == 0)
                    if DialogueButtonPressedInMurmurholdAndSomethingScaryHappens("Enter the castle.", "There are steps chipped in the ice.", "")
                ("Inside the kinetopede.", "Here you can find all miscellaneous options.")
                ("M")
                ("URMURHOLD")
                ("A lonely castle on a cliff. A dark history, the details obscure.")
                    if (oPlayer.LastVisited != "Murmurhold")
                        NeedsCargoItem("Give the Coda a Cryptoglyphic Tablet.", "A tribute, in exchange for your life.", (("[Requires x1 Cryptoglyphic Tablet.You currently have x" + string(CargoReturnNumberOfItems(58))) + ".]")0, 0.8, HasTablet)
                    if (oPlayer.LastVisited != "Murmurhold")
                        NeedsCargoItem("Give the Coda an Ancient Grimoire.", "A tribute, in exchange for your life.", (("[Requires x1 Ancient Grimoire.You currently have x" + string(CargoReturnNumberOfItems(32))) + ".]")0, 0.8, HasGrim)
                    if (oPlayer.Recordkeeper == "Jack of Quills")
                        ("Creep in search of the Coda.")
                if (oPlayer.LastVisited != "Murmurhold" && global.Murmurhold == 0)
                    if DialogueButtonPressedInMurmurholdAndSomethingScaryHappens("Enter the castle.", "There are steps chipped in the ice.", "")
                ("Inside the kinetopede.", "Here you can find all miscellaneous options.")
        else if (DialogueScreen == "SecondScreen" && LeavingTheCity == 1 && TooHeavyToLeaveTheCity == 0)
            ("I")
            ("NTO THE DARK")
            ("Which direction will you go?")
            ("On second thoughts...", "There are matters here that still merit attention.", "#"0, 0.8)
            ("Pore over the map.", "Orient yourself before you launch off into the ravenous dark.")
                    BoxMode = "Map"
            ("North.", "To the gigantic spike of ice that one day tore trough House's wall.")
                oPlayer.LastVisited = "Murmurhold"
                LeavingSpecialRoomDialogueBox("North")
            ("West.", "Nothing but doors, locked and unusable.")
                oPlayer.LastVisited = "Murmurhold"
                LeavingSpecialRoomDialogueBox("West")
            ("East.", "An ancient fungal ruin, far to the east.")
                oPlayer.LastVisited = "Murmurhold"
                LeavingSpecialRoomDialogueBox("East")
            ("South.", "To the Omnipope's moving domain.")
                oPlayer.LastVisited = "Murmurhold"
                LeavingSpecialRoomDialogueBox("South")
        else if (DialogueScreen == "SecondScreen" && LeavingTheCity == 1 && TooHeavyToLeaveTheCity == 1)
        else if (DialogueScreen == "SecondScreen" && (GiveGrimoire == 1 || GiveTablet == 1))
            ("M")
            ("URMURHOLD")
            ("One of the Coda is waiting on the snow-swept battlements to meet you. He smiles a black smile as you show him your tribute.")
                ("Lost x1 Ancient Grimoire", "Cargo", 5)
                ("Lost x1 Cryptoglyphic Tablet", "Occult", 1)
            ReligionText("Nahash", 1, 1)
            ("Barely-there whispers follow you back to the kinetopede.")
                ReligionItemAdd("Nahash", 1, 1)
        else if (DialogueScreen == "SecondScreen" && JackQuest == 1)
        else if (DialogueScreen == "SecondScreen" && WanderTheStreets == 1)
            ("M")
            ("URMURHOLD")
            ("You stroll through the wintry halls of Murmurhold. Empty portrait-frames line the walls. An icy gale howls through glassless windows.")
            ("Just on the edge of hearing, a susurration. A whisper of many voices. You press your ear against the nearest painfully-cold wall, and the whispers become a chorus.")
            ("You know with odd certainty that you are eavesdropping on conversations in other worlds, or many miles away, or that have not yet occurred.")
            ("Keep listening.", "The muttering voices echo through your bones. There are so many of them, they threaten to drown you.", "")
            ("Back away.", "These whispers are not for you. You hurry back to the kinetopede, pulling your coat tight about your shoulders.", "")
        else if (DialogueScreen == "SecondScreen" && Listening1 == 1)
                ("M")
                ("URMURHOLD")
                ("One strand of conversation momentarily drowns out the others. A confession of murder, uttered while staring into a mirror.")
                ("Gained x1 Terrible Secret", "Logbook", 4)
                ("M")
                ("URMURHOLD")
                ("One strand of conversation momentarily drowns out the others. A discussion between a close-knit family of nomads, flying on the back of an airborne turtle in search of their missing mother.")
                ("Gained x1 Glimpse of Another World", "Logbook", 3)
                ("M")
                ("URMURHOLD")
                (((((((("One strand of conversation momentarily drowns out the others. " + '"' + "I know you are listening to me, " + oPlayer.PlayerName) + ".") + '"') + " It is a woman's voice, perfectly calm. ") + '"') + "Desist.") + '"'))
                ("Gained x1 Unsettling Conjecture", "Logbook", 4)
                ("M")
                ("URMURHOLD")
                ("One strand of conversation momentarily drowns out the others. A plot to kill the Prince of Founder's Fire, hashed out over cards and cigars.")
                ("Gained x1 Seditious Whispers: Principate", "Logbook", 4)
            ("Whispers follow you back.", "There is something wrong with the wind here, you're sure of it.", "")
        else if (DialogueScreen == "SecondScreen" && MiscellaneousEvent == 1)
        else if (DialogueScreen == "SecondScreen" && MiscOptions == 1)
            ("C")
            ("APTAIN'S QUARTERS")
            ("The machine greets you with a steady, soothing hum. In your quarters, you can safely decide what side activity to pursue. Deal with prisoners? Drink something? Operate on your own memories, perhaps?")
            ("Return to location.", "")
        else if (DialogueScreen == "LastScreen")
            ("T")
            ("EST")
            ("Successful test.")
            ("Return.", "", "")
    draw_text(((view_wport[(0 << 0)] * 0.5) - 3), ((view_hport[(0 << 0)] * 0.75) - 3), "Murmurhold")
    draw_text((view_wport[(0 << 0)] * 0.5), (view_hport[(0 << 0)] * 0.75), "Murmurhold")
